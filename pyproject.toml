[project]
name = "hearts-llm"
version = "0.1.0"
description = "Measuring context rot in LLMs playing Hearts"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.40",
    "openai>=2.29.0",
    "python-dotenv>=1.2.2",
]

[project.optional-dependencies]
dev = ["pytest"]
